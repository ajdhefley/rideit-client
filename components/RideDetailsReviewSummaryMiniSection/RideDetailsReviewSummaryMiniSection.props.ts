/**
 * 
 **/
export interface RideDetailsReviewSummaryMiniSectionProps {
    /**
     * Unique identifier for coaster whose mini review summary is to be displayed, also serving as coaster's URL subdirectory.
     **/
    coasterUrl: string;
}