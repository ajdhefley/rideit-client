/**
 * 
 **/
export interface RideDetailsCommentSectionProps {
    /**
     * Unique identifier for coaster whose comments are to be displayed, also serving as coaster's URL subdirectory.
     **/
    coasterUrl: string;
}