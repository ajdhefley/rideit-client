/**
 * 
 **/
export interface RideDetailsReviewSectionProps {
    /**
     * Unique identifier for coaster whose reviews are to be displayed, also serving as coaster's URL subdirectory.
     **/
    coasterUrl: string;
}